import React from 'react';
import styles from './SearchResults.module.css';

// Needs a search results list
// Needs a search results item
// Needs a search results item image
// Needs a search results item name
// Needs a search results item artist
// Needs a search results item album


function SearchResults() {
    return (
        <div className={styles.grid}>
            <h2>SearchResults</h2>
            <ul>
                <li>
                    {/* Add TrackList component /> */}
                </li>
            </ul>
        </div>
    );
}

export default SearchResults;